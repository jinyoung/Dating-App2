apiVersion: v1
kind: Service
metadata:
  name: location-based-service-management
  labels:
    app: location-based-service-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: location-based-service-management